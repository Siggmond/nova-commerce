name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Flutter pub get
        run: flutter pub get

      - name: Format (Dart)
        run: dart format --set-exit-if-changed .

      - name: Analyze
        run: flutter analyze

      - name: Test (non-golden)
        run: flutter test --exclude-tags=golden

      # Golden tests are excluded until stable baselines are committed.

      # Release APK builds require Firebase config/signing files and are excluded from public PR CI.

<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Theme applied while the app process is starting -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/nova_splash_bg</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/nova_splash</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
    </style>

    <!-- Theme applied after Flutter has initialized -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

</resources>
